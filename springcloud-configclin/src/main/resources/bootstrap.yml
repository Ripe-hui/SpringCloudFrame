spring:
  cloud:
    config:
      name: springcloud-configclient
      discovery:
        enabled: true
        service-id: springcloud-config
      profile: default
      label: master
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8001/eureka/
