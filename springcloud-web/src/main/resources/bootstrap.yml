spring:
  cloud:
    config:
      name: springcloud-web
      discovery:
        enabled: true
        service-id: springcloud-config
      profile: default
      label: master
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8001/eureka/